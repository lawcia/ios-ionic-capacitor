import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.lawcia',
  appName: 'my-ionic-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
